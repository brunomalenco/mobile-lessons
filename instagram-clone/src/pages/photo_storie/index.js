import { View, Text, Image, StyleSheet } from "react-native";

export default function PhotoStorie(){
    return(
        <View style={{justifyContent: 'center', alignItems: 'center'}}>
            Foto
        </View>
    )
}